import './theme-mode.css';

const ThemeMode = ({theme}) => {
    return (
        <div className='theme-mode' id={theme}>
            <div className='theme-mode__item' id={theme}>
                
            </div>
        </div>
    );
}

export default ThemeMode;